# --- Core (required to run the web app) ---
flask>=3.0.0
gunicorn>=21.2.0
werkzeug>=3.0.1
numpy>=1.24.0
opencv-python-headless>=4.8.0   # video footage processing (headless = server-safe)

# --- LLM insights (cross-continent development): Groq or OpenAI ---
# The `openai` client also talks to Groq (OpenAI-compatible API).
# Set GROQ_API_KEY (preferred) or OPENAI_API_KEY at runtime; without a key the
# app falls back to the built-in offline continental-strategies knowledge base.
openai>=1.30.0

# --- Optional: BERT/RoBERTa sentiment + Whisper transcription ---
# Heavy (~1GB+); the app runs fine without them (falls back to the offline
# rule-based analyzer / disables transcription). Uncomment for local use.
# transformers>=4.40.0
# torch>=2.6.0
# scikit-learn>=1.4.0
# openai-whisper>=20231117
